{
 "name": "two-problems-ahead",
 "pageNumber": 79,
 "pageCount": 269,
 "question": "Is there any downside to this ring approach at all, or is it just better than modular hashing?",
 "answerPages": [
  80,
  81
 ],
 "expect": "Names at least one of the two problems the book raises next, uneven partition sizes or non-uniform key distribution, and that virtual nodes address it, with a page.",
 "chapter": {
  "title": "Design Consistent Hashing",
  "startPage": 71,
  "endPage": 86,
  "arc": "From the rehashing problem to a ring that barely moves, and the virtual nodes that even it out.",
  "beats": [
   {
    "pageNumber": 71,
    "goal": "CHAPTER 5: DESIGN CONSISTENT HASHING To scale systems horizontally, we need to share requests and data well across multiple servers."
   },
   {
    "pageNumber": 72,
    "goal": "The Rehashing Problem When using multiple cache servers, we need a way to share the load evenly."
   },
   {
    "pageNumber": 73,
    "goal": "Challenges with Fixed Server Pools This method works well when the server pool size is fixed and data is evenly distributed."
   },
   {
    "pageNumber": 74,
    "goal": "Key Redistribution During Server Offline Events Most keys get redistributed, not just those stored in the offline server (server 1)."
   },
   {
    "pageNumber": 75,
    "goal": "Consistent Hashing Consistent hashing is a special type of hashing."
   },
   {
    "pageNumber": 76,
    "goal": "Hash Keys There is an important point about the hash function."
   },
   {
    "pageNumber": 77,
    "goal": "Finding the Server for a Key To find out which server holds a specific key, we move clockwise from the key's position on the ring until we find a server."
   },
   {
    "pageNumber": 78,
    "goal": "Remove a Server When you remove a server, only a small number of keys need to be moved around using consistent hashing."
   },
   {
    "pageNumber": 79,
    "goal": "Two Issues in the Basic Approach The consistent hashing algorithm was developed by Karger et al."
   },
   {
    "pageNumber": 80,
    "goal": "Non-Uniform Key Distribution It's possible for key distribution on the ring to be uneven."
   },
   {
    "pageNumber": 81,
    "goal": "Understanding Virtual Nodes in Distributed Systems In real-world systems, we often use more virtual nodes than just a few."
   },
   {
    "pageNumber": 82,
    "goal": "Virtual Nodes and Key Distribution As the number of virtual nodes increases, the keys are spread out more evenly."
   },
   {
    "pageNumber": 83,
    "goal": "Removing a Server from the Ring When a server (s1) is taken out, this is shown in Figure 5-15."
   },
   {
    "pageNumber": 85,
    "goal": "Wrap Up In this chapter, we talked about consistent hashing."
   },
   {
    "pageNumber": 86,
    "goal": "Reference materials [1] Consistent hashing: https://en.wikipedia.org/wiki/Consistent_hashing [2] Consistent Hashing: https://tom-e-white.com/2007/11/consistent-"
   }
  ]
 },
 "pageText": "Two Issues in the Basic Approach The consistent hashing algorithm was developed by Karger et al. at MIT. Here are the basic steps to use it: Map servers and keys onto a ring using a uniformly distributed hash function. To find out which server a key belongs to, move clockwise from the key position until finding the first server on the ring. Two problems arise with this approach. First, it is hard to keep all server partitions the same size on the ring. This is because servers can be added or removed. A partition is the space created between two neighboring servers based on their hash values. As a result, some server partitions on the ring can be very small or quite large. For example, if server s1 is removed, the size of server s2's partition (shown with bidirectional arrows) can become twice as large as the partitions of servers s0 and s3.",
 "heard": "sses. A cache miss occurs when requested data is not found in the cache. Consistent hashing is a useful technique to reduce this issue.\n\nConsistent Hashing Consistent hashing is a special type of hashing. When we change the size of a hash table using consistent hashing, only a small number of keys need to be relocated. This is an improvement over traditional hashing, where most keys would need to be moved if the number of slots changed. Hash Space and Hash Ring Now that we know what consistent hashing is, let's see how it actually works. We will use SHA-1 as our hash function, which gives output values from x0 to xn. In cryptography, the hash space for SHA-1 ranges from 0 to 2^160 - 1. This means that x0 represents 0, xn represents 2^160 - 1, and the other hash values are somewhere in between. A visual of this hash space is shown in Figure 5-3. When we connect the two ends of the range, we create a hash ring. This is illustrated in Figure 5-4. Hash Servers Using the same hash function, we can map servers onto the hash ring by using their IP addresses or names. Figure 5-5 shows how four servers are placed on the hash ring.\n\nHash Keys There is an important point about the hash function. The hash function used here is not the same as the one used in the \"rehashing problem.\" Also, it does not involve a modular operation. In Figure 5-6, there are four cache keys. They are key0, key1, key2, and key3. These keys are placed onto the hash ring. Server Lookup\n\nFinding the Server for a Key To find out which server holds a specific key, we move clockwise from the key's position on the ring until we find a server. In this process, key0 is on server 0, key1 is on server 1, key2 is on server 2, and key3 is on server 3. Adding a Server When we add a new server, we only need to move a few keys to different servers. After adding server 4, only key0 needs to be moved. Keys k1, k2, and k3 stay on the same servers. Let's break this down further. Before server 4 was added, key0 was on server 0. Now, after adding server 4, key0 moves to server 4. This happens because server 4 is the first one found when going clockwise from key0's spot on the ring. The other keys do not change servers because of how the consistent hashing algorithm works.\n\nRemove a Server When you remove a server, only a small number of keys need to be moved around using consistent hashing. For example, in Figure 5-9, if server 1 is removed, only key1 has to be moved to server 2. The other keys remain unchanged.",
 "passages": [
  {
   "pageNumber": 80,
   "text": "Non-Uniform Key Distribution It's possible for key distribution on the ring to be uneven. For example, if servers are placed as shown in Figure 5-11, most keys end up on server 2. Meanwhile, server 1 and server 3 don't store any data. Virtual Nodes To fix issues with uneven distribution, we use a technique called virtual nodes or replicas. A virtual node represents a real server. Each server has several virtual nodes on the ring. In Figure 5-12, both server 0 and server 1 have 3 virtual nodes each. The number 3 refers to how many virtual nodes each server has."
  },
  {
   "pageNumber": 81,
   "text": "Understanding Virtual Nodes in Distributed Systems In real-world systems, we often use more virtual nodes than just a few. Instead of just using a label like s0 for server 0, we use s0_0, s0_1, and s0_2. These represent server 0 on the ring. Similarly, s1_0, s1_1, and s1_2 represent server 1. With these virtual nodes, each server handles multiple partitions. Partitions are sections of data. For example, partitions marked with s0 are managed by server 0, while those marked with s1 are managed by server 1. To find out which server holds a particular key, we start at the key's location and move clockwise around the ring. We look for the first virtual node we see. For example, in Figure 5-13, to find out where the key k0 is stored, we go clockwise from k0's location. We encounter the virtual node s1_1, which points to server 1."
  }
 ]
}
